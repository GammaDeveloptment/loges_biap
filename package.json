{
  "name": "loges-biap",
  "private": true,
  "version": "0.1.0",
  "description": "Loges-BIAP - Inteligencia Comercial y Logistica, Grupo Gammacargo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace=packages/shared-types",
    "dev:api": "npm run build:shared && npm run start:dev --workspace=apps/api",
    "dev:web": "npm run build:shared && npm run dev --workspace=apps/web",
    "build": "npm run build:shared && npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  }
}
